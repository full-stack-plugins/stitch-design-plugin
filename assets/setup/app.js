const card=document.querySelector(".setup-card");const input=document.querySelector("#api-key");const save=document.querySelector("#save");const launch=document.querySelector("#launch");const status=document.querySelector("#status");const csrf=card.dataset.csrf;
async function post(path,body){const response=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({csrfToken:csrf,...body})});return response.ok}
save.addEventListener("click",async()=>{const apiKey=input.value.trim();if(!apiKey){status.textContent="请先填写 API Key。";return}save.disabled=true;try{const ok=await post("/api/save",{apiKey});input.value="";status.textContent=ok?"保存成功，可以打开 Codex。":"保存失败，请检查后重试。";launch.disabled=!ok}catch{input.value="";status.textContent="保存失败，请确认本地设置页仍在运行。"}finally{save.disabled=false}});
launch.addEventListener("click",async()=>{launch.disabled=true;const ok=await post("/api/launch",{});status.textContent=ok?"Codex 已启动。":"无法自动启动，请使用高级设置中的命令。";if(!ok)launch.disabled=false});
